import 'package:flutter/material.dart';

class InputMessagePage extends StatelessWidget {
  const InputMessagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: [
            Text('Input'),
          ],
        ),
      ),
    );
  }
}
